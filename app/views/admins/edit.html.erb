<%= simple_form_for @admin do |f| %>
    <%= f.input :webform, :label => 'Organization uuid' %>
    <%= f.input :uuid, :label => 'UUID' %>
    <%= f.input :name, :label => 'Name' %>
    <%= f.button :submit %>
<% end %>
